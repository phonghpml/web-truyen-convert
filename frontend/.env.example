# Local development
NEXT_PUBLIC_CRAWLER_URL=http://127.0.0.1:8000

# Production on Vercel: keep same-origin request to avoid CORS
# NEXT_PUBLIC_CRAWLER_URL=/api

# Optional override for Next rewrite target when deploying to Vercel
# API_BASE_URL=https://phonghp-crawler.hf.space